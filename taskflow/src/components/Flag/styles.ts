import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    width: 80,
    height: 30,
    justifyContent: "center",
    borderRadius: 7,
    elevation: 5,
  },
  caption: {
    color: "#fff",
    textAlign: "center",
  },
});
